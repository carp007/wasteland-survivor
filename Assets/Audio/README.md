# Assets/Audio

This folder holds all project audio.

Conventions:
- Drop raw downloads into `_INBOX/`.
- Create trimmed/looped/normalized files in `_PROCESSED/`.
- Only reference `_PROCESSED/` assets in scenes/scripts.

Licensing manifest lives in `Docs/Audio/ATTRIBUTION_AND_LICENSES.md`.
